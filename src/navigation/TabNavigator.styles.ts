import { StyleSheet } from 'react-native';
import { TAB_BAR } from '@/constants/dimensions';

export const tabNavigatorStyles = StyleSheet.create({
  tabBarIcon: {
    fontSize: TAB_BAR.iconFontSize,
  },
});
